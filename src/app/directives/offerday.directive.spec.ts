import { OfferdayDirective } from './offerday.directive';

describe('OfferdayDirective', () => {
  it('should create an instance', () => {
    const directive = new OfferdayDirective();
    expect(directive).toBeTruthy();
  });
});
